name: Build & Deploy

on:
  push:
    branches:
      - main

env:
  REGISTRY: git.rfa.cz
  IMAGE_NAME: tth/birdnet-go

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        id: build
        run: |
          SHORT_SHA=$(echo ${{ github.sha }} | cut -c1-8)
          echo "short_sha=sha-${SHORT_SHA}" >> $GITHUB_OUTPUT

          # Build version from git
          BUILD_VERSION="$(git describe --tags --always 2>/dev/null || echo "sha-${SHORT_SHA}")"

          docker build \
            --build-arg BUILD_VERSION="${BUILD_VERSION}" \
            --platform linux/amd64 \
            -t ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest \
            -t ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:sha-${SHORT_SHA} \
            .

      - name: Push to registry
        run: |
          echo "${{ secrets.REGISTRY_TOKEN }}" | docker login ${{ env.REGISTRY }} -u ${{ gitea.actor }} --password-stdin
          docker push ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
          docker push ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.build.outputs.short_sha }}

      - name: Deploy to Portainer
        run: |
          PORTAINER_URL="${{ secrets.PORTAINER_URL }}"
          PORTAINER_TOKEN="${{ secrets.PORTAINER_TOKEN }}"
          STACK_ID="${{ secrets.PORTAINER_STACK_ID }}"
          IMAGE_TAG="${{ steps.build.outputs.short_sha }}"

          COMPOSE=$(cat docker-compose.swarm.yml)

          # Fetch existing env vars (secrets stored in Portainer)
          STACK_RESPONSE=$(curl -s -w "\n%{http_code}" \
            -H "X-API-Key: ${PORTAINER_TOKEN}" \
            "${PORTAINER_URL}/api/stacks/${STACK_ID}")
          HTTP_CODE=$(echo "$STACK_RESPONSE" | tail -1)
          STACK_BODY=$(echo "$STACK_RESPONSE" | sed '$d')

          if [ "$HTTP_CODE" != "200" ]; then
            echo "Portainer GET /api/stacks/${STACK_ID} returned HTTP ${HTTP_CODE}"
            echo "$STACK_BODY" | head -5
            exit 1
          fi

          EXISTING_ENV=$(echo "$STACK_BODY" | python3 -c "import sys,json; print(json.dumps(json.load(sys.stdin).get('Env', [])))")

          # Merge IMAGE_TAG into existing env vars
          EXISTING_ENV="$EXISTING_ENV" IMAGE_TAG_VAL="$IMAGE_TAG" python3 -c "
          import json, sys, os
          compose = sys.stdin.read()
          existing = json.loads(os.environ['EXISTING_ENV'])
          merged = [e for e in existing if e.get('name') != 'IMAGE_TAG']
          merged.append({'name': 'IMAGE_TAG', 'value': os.environ['IMAGE_TAG_VAL']})
          payload = {
              'stackFileContent': compose,
              'env': merged,
              'prune': False,
              'pullImage': True,
          }
          print(json.dumps(payload))
          " <<< "$COMPOSE" | curl -s -w "\n%{http_code}" -X PUT \
            "${PORTAINER_URL}/api/stacks/${STACK_ID}?endpointId=1" \
            -H "X-API-Key: ${PORTAINER_TOKEN}" \
            -H "Content-Type: application/json" \
            -d @- | {
              BODY=$(cat)
              HTTP=$(echo "$BODY" | tail -1)
              RESP=$(echo "$BODY" | sed '$d')
              if [ "$HTTP" != "200" ]; then
                echo "Stack PUT returned HTTP ${HTTP}: ${RESP}" | head -3
                exit 1
              fi
              echo "Stack update triggered (IMAGE_TAG=${IMAGE_TAG})"
            }

      - name: Wait for service health
        run: |
          echo "Waiting for BirdNET-Go to start..."
          for i in $(seq 1 30); do
            sleep 10
            # Check via HAProxy (birds.rfa.cz) or direct port
            STATUS=$(curl -so /dev/null -w '%{http_code}' --insecure "https://birds.rfa.cz/health" 2>/dev/null || echo "000")
            echo "Attempt $i/30: HTTP $STATUS"
            if [ "$STATUS" = "200" ]; then
              echo "BirdNET-Go is up"
              break
            fi
            if [ "$i" = "30" ]; then
              echo "BirdNET-Go did not come up after ~5 minutes"
              exit 1
            fi
          done

      - name: Smoke test
        run: |
          HEALTH=$(curl -sf --insecure "https://birds.rfa.cz/health" 2>/dev/null || echo "{}")
          echo "Health: ${HEALTH}"
          if echo "$HEALTH" | python3 -c "import sys,json; d=json.load(sys.stdin); assert d.get('status')=='healthy'" 2>/dev/null; then
            echo "Health check passed"
          else
            echo "Health check failed or degraded"
            exit 1
          fi
